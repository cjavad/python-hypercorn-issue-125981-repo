# python-hypercorn-issue-125981-repo

To run the repo:

```bash
pipenv install
# Use two terminals or tmux / similar to run both commands
pipenv run python app.py
pipenv run python -m drivers firefox
```